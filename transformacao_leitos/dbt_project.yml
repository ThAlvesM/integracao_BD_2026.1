name: 'transformacao_leitos'
version: '1.0.0'
config-version: 2

profile: 'transformacao_leitos'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  transformacao_leitos:
    staging:
      +materialized: view
    marts:
      +materialized: table